# Changes since the pause (2026-08-26 → 2026-08-28)

Width-gradient programme resume, this session. Commits: `747b134`, `608d4a1` (+ pending: smoke tier, CLAUDE.md rule).

## Dispatched / running
- **Athena job 137853**: task **41** = λ-chain toy (3 iterates, uniform seed, `wg_lam_chain=True`) + task **34** = control twin (identical, chain OFF, current engine+mesh). ~11 h; auto-proceed to the 96 h campaign if the toy passes.
- First attempt (137845) failed and was root-caused the same night — see "Bugs fixed".

## Bugs fixed
1. **Analysis-mode dEps crash** (killed 137845_41 at 1:02): the λ-chain selector conversions ran inside `calculate_gradient_fields` while the CAD was in analysis mode. Moved to the driver site (where the width conversion provably works). Plumbing gate now asserts, from source, it cannot creep back.
2. **Task-index collision** (137845_27 ran a GPU probe rung instead of the control): index 27 was captured by the `_GFR_RUNGS` membership branch. Control moved to 34; lesson noted in code.

## New guards in the optimizer (from the literature check)
- `wgp_lam_step_nm` — per-step resonance-drift bound (0.5 nm in the campaign spec); a step whose peak jumps more is rejected like a filter fail.
- **Curvature floor** — the IFT chain term skips loudly if |dTp| < 5 % of its first healthy value (unbounded-gain hole), warns at 25 %.
- `proj_rot_deg` logging — rotation of the projected direction under ∂W/∂λ ×0.8→×1.2, pricing the path-fitted-coefficient risk every iterate.

## New optimizer option: RGP (off by default)
Relaxed gradient projection (Antonau et al., SMO 2021): one continuous formula replaces CLIMB/RIDE/RESTORE — buffer-ramped projection weight, bounded rotating correction, history-sized buffer, zigzag + infeasible-drift adaptation. Endpoint proven identical to today's RIDE (projection gate, now 22 checks). Adopt in campaign 2 or on restore-thrash. Extracted policy: `runners/lumopt2_design/notes_relaxed_projection.md`.

## New debug infrastructure
- **Pipeline smoke tier** — `validate_c325` task 35: same spec/code paths on an N=40 low-Q surrogate, 2 iterates, ~1.5–2 h. Mandatory between offline gates and any hours-scale dispatch (catches live-session-state bugs the gates cannot). Rule added to CLAUDE.md §5 + skill item 39.
- Plumbing gate hardened (comment-stripped source-structure checks).

## Ops
- **Fixed h5 cleaner installed on Athena** (old broken one replaced, md5-verified; quota 199G/300G).
- Both queues probed, seats 0/50; q3db N=100–220 already local; no N=240/280/320 results exist anywhere (137331 left nothing — still owed a `sacct` query).

## Analyses (zero GPU, Opus subagents, Fable-reviewed)
- **λ-detrend re-read of 78 eval logs**: stored slopes reproduce exactly (0.3654 / 0.300), but a shift-frozen run grew +0.54 µm at fixed λ ⇒ **dW/dλ is a path derivative, not a device constant**; T rose +1.6 pp at fixed λ by spending width — the projection nulls exactly that channel, so the toy stays decisive.
- **Method literature check**: chain rule published (arXiv 2511.16643); projection = published null-space flow (Feppon 2020); width functional novel; C_field phase flagged for eventual analytic derivation. Reading list + lessons: `memory/reference_method_lit_check_2026-08-27.md`.
- **Token audit of the last 2 weeks**: 96 % of burn = two marathon sessions; 13 % = manual queue polling. Fixes are standing rules (skill item 38): Fable decides, Opus subagents execute, Monitor scripts poll, batched doc edits.

## Docs / memory
- HANDOFF top box rewritten (resume state); skill items 37–39; memory unpaused + short-answers rule; RGP policy note.
