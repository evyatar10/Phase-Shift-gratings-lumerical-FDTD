# CHANGES 2026-08-31 → 09-01 — the d1 generation's first full day

Study dir: runners/lumopt2_design/ | Jobs: 139049/139050 (died), 139225 (d1),
139226 (d1u), 139244 (voided smoke), 139345 (reuse smoke), 139256 (angle probe)
| Written during the overnight work-alone session; state authority =
HANDOFF.md top box + memory project_v2_width_gradient_plan.md top boxes.

## Verdicts (all MEASURED unless noted)

1. **numpy-2 `.ptp()` crash**: d1u's first incarnation (139050) died at
   11:39 h inside the online dwdlam refit (`lams.ptp()`; ndarray method
   removed in NumPy 2; container=2.x, IGUM=1.x masked it; every gate/toy ran
   ≤4 accepted points, refit engages at n≥5). Fixed (np.ptp ×2), both lanes
   relaunched same hour as 139225/139226 — resume + optstate carried
   everything (d1's recomputed gradient matched pre-crash to ~13 digits).
   Lesson → CLAUDE.md §5 second corollary (ENGAGEMENT CONDITIONS checked on
   paper before any dispatch).
2. **d1 surpassed the hand benchmark**: t_pk 0.96348→**0.96762** (+0.0041
   over BEST_T9636) at λ 1566.444 EXACT every eval, W in-band, Q_i
   109.7k→123.7k. Includes the programme's FIRST filter reject (33.75 nm
   step degraded fom; cap halved; retry from stored gradients delivered the
   new best — the trust region found the ridge scale itself).
3. **d1u re-derived near-BEST from uniform in a DIFFERENT basin**:
   0.94254→**0.96209** (biggest single step +0.0078 at cap 33.75; corrected
   sequence in memory), corr profile sub-uniform (mean 321, inner teeth cut
   to 268) vs BEST family's super-uniform 358 — two solution families exist.
   λ residual grew to 0.30 nm under 50.6 nm caps (second-order leak;
   restoration + 0.5 nm reject bound self-limit; per user λ-policy this is
   acceptable while T climbs).
4. **Width-gradient rotation MEASURED: 0.685° per 10 nm step** (cos
   0.999929; probe task 53, job 139256, production N=100 numerics; vectors:
   results_from_athena/v2_ns2_toy/gW_angle_{A,B}.npy). ⇒ reuse k=5 approved
   (user); adjoint-parallelization rejected at k=5 (≤6% residual win).
5. **Reuse skip works on hardware** (smoke 139345, N=60 surrogate):
   `width row REUSED — width adjoint skipped`, step held both constraints,
   cap grew. Final semantic verdict: see appendix (pending at first write).
6. **T-vs-width decomposition of the jump** (user question): via
   t_pk=(1−Q_L/Q_i)², the +0.0078 jump step at fixed W would have gained
   MORE (0.96008 vs 0.95983) — the commanded W growth (toward the lane's
   18.613 ride target) slightly cost T; 100% of the gain was Q_i.
7. **Cap physics confirmed**: gain ~linear in cap, leak ~quadratic — the
   adaptive growth found ~34 nm as the sweet spot; future lanes may START
   at 20-30 (ceiling 60).
8. **Comparisons vs Itai (family-level; his rows CONFORMAL, ours PVA — do
   not cross-quote in writeups)**: his Nt60-native-20µm TE: T 0.975 /
   Q_L 7,680 / Q_i 610k@N98, ≥1.16M@N130 (unsaturated). TE/TM factor 3.4×
   measured same-geometry. Beating his device = Q_i > 1.16M = T 0.9966 @
   Q_L 2000 ⇒ TE lane with our machinery; TM realistic target 150-250k via
   N_FREE 25→60 + freed comb.

## Engine/infra changes (committed af40902 + 744b4f1; later doc edits parked)

- ns2 near-null guard (rho_T<0.02 scales step), recenter clears λ-target/
  dTp0, reuse drift referenced to last-fresh solve (k-proof), angle-probe
  accepted-pair guard, filtered-queue behavioral gate, dT_pred logging
  (predictive stopping), gW_refresh_cos logging (adaptive-k calibration
  data), optstate sidecar (cap/λ-target/reuse_age, REQUEUE-proof).
- Watcher discipline: state-only change keys (elapsed-time keys cost ~1
  wake/poll); VPN-outage and SLURM_CTL_DOWN labeled states.
- New rules: CLAUDE.md §5 engagement-conditions corollary; §6 optimizer
  lanes inherit state across labels + result identity = engine version +
  numerics (never cluster) + "can't verify" is never a rerun reason.

## Overnight incident log (work-alone session)

- **~02:00: d1 (139225) FAILED at 12:35 h — the `.ptp()` AttributeError AGAIN**,
  in a build whose SOURCE was grep-verified fixed on the server before
  dispatch. It fired exactly at the dwdlam-refit n≥5 engagement (d1's λ is
  pinned so unique-pair count grows with evals). Traceback source lines are
  garbled (running process's line numbers rendered against the newer on-disk
  file — a known deploy-while-running display artifact). **Prime suspect:
  STALE `__pycache__` BYTECODE surviving rsync** (remote pycache held mixed
  cpython-313/314 pycs; pyc validation is mtime+size and rsync can preserve
  a same-size window). Counter-evidence noted honestly: d1u shares the build
  and its refit... its λ moves, so its unique-pair set differs — it may
  simply not have crossed the same branch. Mechanism = PLAUSIBLE, not
  CONFIRMED.
- **Recovery (~02:05):** d1's 13 eval rows fetched local (best eval 4 =
  0.96762 intact); all remote .py mtimes bumped via `touch` (invalidates
  every pyc without a delete-prompt); d1 resubmitted as **139520** — which
  therefore starts on the k=5 restart config (silver lining: the lane got
  its restart without any scancel). Watcher re-armed (v3) covering
  139520/139226/139516 with AttributeError added to the error greps.
- **Standing lesson (candidate rule): after any deploy, verify not only the
  .py content but consider bytecode freshness — `touch` the tree or clear
  pycache when a "fixed" error recurs; and a fix to a count-triggered branch
  is only PROVEN once a run has crossed the trigger count on the new build.**

## Appendix — filled at the hour-11 handoff

- Reuse smoke final verdict: (pending)
- 24h checkpoint numbers: (pending)
- Compaction audit: DONE (read-only) → **COMPACTION_PLAN.md** (same dir).
  Executed same-session (SAFE-NOW): stale "task 35 = smoke" doc pointer
  fixed in CLAUDE.md §5 + skill item 39 (was aiming at a CUDA-probe rung);
  gates/run_all_gates.py added + verified green; b1 seed-lesson lifted into
  THEORY.md §6d. Everything touching runners/*.py is BOUNDARY-ONLY (REQUEUE
  re-imports the deployed tree); HANDOFF compaction deferred (parallel
  session appends to it). Deletes (scratch_s5vec.txt, campaign_v2_proj_b2,
  optional RGP bundle) parked for approval.
